// import { NameSpace } from '../../const';
// import { State } from '../../types/state';
// import { OfferList } from '../../types/types';

// export const getFavoriteOffers = (state: State): OfferList => state[NameSpace.Favorite].favoriteOffers;
// // export const getFavoriteStatus = (state: State): boolean => state[NameSpace.Favorite].favoriteOffers.some((offer) => offer.id === );

